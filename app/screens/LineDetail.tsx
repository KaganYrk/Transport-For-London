import React from 'react';
import { ImageBackground, View } from 'react-native';
import Layout from '../components/Layout';

export default function LineDetail({ navigation, props }) {
  return (
    <Layout>
    </Layout>
  );
}
